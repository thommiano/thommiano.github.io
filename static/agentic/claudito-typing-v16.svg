<!--
  Prompt: "Can you scale the keyboard down to make it smaller"

  Scaled down smaller keyboard.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 120" width="400" height="240">
  <defs>
    <linearGradient id="keyTop" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" style="stop-color:#666"/>
      <stop offset="100%" style="stop-color:#444"/>
    </linearGradient>
  </defs>

  <style>
    .body { fill: #dc7974; }
    .keyboard-base { fill: #0a0a0a; }
    .key-well { fill: #111; }
    .key-cap { fill: url(#keyTop); }
    .tentacle {
      fill: none;
      stroke: #dc7974;
      stroke-width: 4;
      stroke-linecap: round;
    }
  </style>

  <!-- Keyboard base - 16:9 ratio, scaled down -->
  <rect class="keyboard-base" x="68" y="64" width="64" height="36" rx="2"/>
  <rect class="key-well" x="70" y="66" width="60" height="32" rx="1"/>

  <!-- Keys - full keyboard, scaled down -->
  <!-- Row 1: Spacebar row - y=68, closest -->
  <rect class="key-cap" x="72" y="68" width="5" height="4" rx="0.5"/>
  <rect class="key-cap" x="78" y="68" width="5" height="4" rx="0.5"/>
  <rect class="key-cap" x="84" y="68" width="32" height="4" rx="0.5"/>
  <rect class="key-cap" x="117" y="68" width="5" height="4" rx="0.5"/>
  <rect class="key-cap" x="123" y="68" width="5" height="4" rx="0.5"/>

  <!-- Row 2: ZXCV row - y=74 -->
  <rect class="key-cap" x="72" y="74" width="6" height="4" rx="0.5"/>
  <rect class="key-cap" x="79" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="84" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="89" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="94" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="99" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="104" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="109" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="114" y="74" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="119" y="74" width="8" height="4" rx="0.5"/>

  <!-- Row 3: ASDF row - y=80 -->
  <rect class="key-cap" x="72" y="80" width="7" height="4" rx="0.5"/>
  <rect class="key-cap" x="80" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="85" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="90" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="95" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="100" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="105" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="110" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="115" y="80" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="120" y="80" width="7" height="4" rx="0.5"/>

  <!-- Row 4: QWERTY row - y=86 -->
  <rect class="key-cap" x="72" y="86" width="5" height="4" rx="0.5"/>
  <rect class="key-cap" x="78" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="83" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="88" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="93" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="98" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="103" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="108" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="113" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="118" y="86" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="123" y="86" width="4" height="4" rx="0.5"/>

  <!-- Row 5: Number row - y=92, furthest -->
  <rect class="key-cap" x="72" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="77" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="82" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="87" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="92" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="97" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="102" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="107" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="112" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="117" y="92" width="4" height="4" rx="0.5"/>
  <rect class="key-cap" x="122" y="92" width="5" height="4" rx="0.5"/>

  <!-- Tentacle legs -->
  <path id="leg0" class="tentacle"/>
  <path id="leg1" class="tentacle"/>
  <path id="leg2" class="tentacle"/>
  <path id="leg3" class="tentacle"/>

  <!-- Claudito body -->
  <g id="claudito" transform="translate(100, 18)">
    <g class="body">
      <rect x="-28" y="0" width="4" height="16"/>
      <rect x="-24" y="0" width="8" height="8"/>
      <rect x="-24" y="8" width="4" height="8"/>
      <rect x="-16" y="0" width="24" height="16"/>
      <rect x="8" y="0" width="8" height="8"/>
      <rect x="12" y="8" width="4" height="8"/>
      <rect x="16" y="0" width="4" height="16"/>
    </g>

    <g class="body">
      <rect x="-36" y="16" width="4" height="8"/>
      <rect x="-32" y="16" width="8" height="8"/>
      <rect x="-28" y="24" width="4" height="8"/>
      <rect x="-24" y="16" width="40" height="16"/>
      <rect x="16" y="16" width="8" height="8"/>
      <rect x="16" y="24" width="4" height="8"/>
      <rect x="24" y="16" width="4" height="8"/>
    </g>
  </g>

  <script type="text/javascript"><![CDATA[
    class PerlinNoise {
      constructor(seed = Math.random() * 10000) {
        this.p = [];
        for (let i = 0; i < 256; i++) this.p[i] = i;
        let n = seed;
        for (let i = 255; i > 0; i--) {
          n = (n * 16807) % 2147483647;
          const j = n % (i + 1);
          [this.p[i], this.p[j]] = [this.p[j], this.p[i]];
        }
        this.p = this.p.concat(this.p);
      }
      fade(t) { return t * t * t * (t * (t * 6 - 15) + 10); }
      lerp(a, b, t) { return a + t * (b - a); }
      grad(hash, x) { return (hash & 1) === 0 ? x : -x; }
      noise1D(x) {
        const xi = Math.floor(x) & 255;
        const xf = x - Math.floor(x);
        const u = this.fade(xf);
        return this.lerp(this.grad(this.p[xi], xf), this.grad(this.p[xi + 1], xf - 1), u);
      }
    }

    // Key positions (center of each key) - scaled down keyboard
    const allKeys = [
      // Row 1: Spacebar row - y=70, closest
      { x: 74.5, y: 70 }, { x: 80.5, y: 70 }, { x: 100, y: 70 }, { x: 119.5, y: 70 }, { x: 125.5, y: 70 },
      // Row 2: ZXCV row - y=76
      { x: 75, y: 76 }, { x: 81, y: 76 }, { x: 86, y: 76 }, { x: 91, y: 76 }, { x: 96, y: 76 },
      { x: 101, y: 76 }, { x: 106, y: 76 }, { x: 111, y: 76 }, { x: 116, y: 76 }, { x: 123, y: 76 },
      // Row 3: ASDF row - y=82
      { x: 75.5, y: 82 }, { x: 82, y: 82 }, { x: 87, y: 82 }, { x: 92, y: 82 }, { x: 97, y: 82 },
      { x: 102, y: 82 }, { x: 107, y: 82 }, { x: 112, y: 82 }, { x: 117, y: 82 }, { x: 123.5, y: 82 },
      // Row 4: QWERTY row - y=88
      { x: 74.5, y: 88 }, { x: 80, y: 88 }, { x: 85, y: 88 }, { x: 90, y: 88 }, { x: 95, y: 88 },
      { x: 100, y: 88 }, { x: 105, y: 88 }, { x: 110, y: 88 }, { x: 115, y: 88 }, { x: 120, y: 88 }, { x: 125, y: 88 },
      // Row 5: Number row - y=94, furthest
      { x: 74, y: 94 }, { x: 79, y: 94 }, { x: 84, y: 94 }, { x: 89, y: 94 }, { x: 94, y: 94 },
      { x: 99, y: 94 }, { x: 104, y: 94 }, { x: 109, y: 94 }, { x: 114, y: 94 }, { x: 119, y: 94 }, { x: 124.5, y: 94 }
    ];

    // Keys grouped by region for each tentacle - smaller keyboard
    const keyRegions = [
      // Leg 0: left side
      allKeys.filter(k => k.x < 92),
      // Leg 1: left-center
      allKeys.filter(k => k.x >= 82 && k.x < 105),
      // Leg 2: right-center
      allKeys.filter(k => k.x >= 95 && k.x < 118),
      // Leg 3: right side
      allKeys.filter(k => k.x >= 108)
    ];

    // Tap animation curve
    function tapCurve(progress) {
      if (progress < 0.12) {
        // Anticipation: pull back
        const t = progress / 0.12;
        return { reach: -0.15 * Math.sin(t * Math.PI), curl: -0.3 * t };
      } else if (progress < 0.35) {
        // Strike: fast move to key
        const t = (progress - 0.12) / 0.23;
        const ease = t * t;
        return { reach: ease, curl: 0.4 * ease };
      } else if (progress < 0.5) {
        // Impact hold
        const t = (progress - 0.35) / 0.15;
        return { reach: 1.0, curl: 0.4 - 0.1 * Math.sin(t * Math.PI) };
      } else {
        // Return
        const t = (progress - 0.5) / 0.5;
        const ease = 1 - (1 - t) * (1 - t);
        return { reach: 1 - ease, curl: 0.3 * (1 - ease) };
      }
    }

    function lerp(a, b, t) {
      return a + (b - a) * t;
    }

    const clauditoX = 100;
    const clauditoY = 18;

    const tentacles = [
      {
        el: document.getElementById('leg0'),
        anchorX: clauditoX - 18,
        anchorY: clauditoY + 34,
        // Hover position (above keys)
        hoverX: 80,
        hoverY: 60,
        keys: keyRegions[0],
        noise: {
          curl1: new PerlinNoise(1),
          curl2: new PerlinNoise(11),
          sway: new PerlinNoise(21),
          hover: new PerlinNoise(31),
          keySelect: new PerlinNoise(41)
        },
        phase: 0,
        tapping: false,
        tapProgress: 0,
        tapDuration: 0.45,
        lastTapTime: 0,
        nextTapDelay: 0.08,
        targetKey: null,
        preTapX: 0,
        preTapY: 0
      },
      {
        el: document.getElementById('leg1'),
        anchorX: clauditoX - 10,
        anchorY: clauditoY + 34,
        hoverX: 92,
        hoverY: 60,
        keys: keyRegions[1],
        noise: {
          curl1: new PerlinNoise(2),
          curl2: new PerlinNoise(12),
          sway: new PerlinNoise(22),
          hover: new PerlinNoise(32),
          keySelect: new PerlinNoise(42)
        },
        phase: 1.3,
        tapping: false,
        tapProgress: 0,
        tapDuration: 0.40,
        lastTapTime: 0.12,
        nextTapDelay: 0.06,
        targetKey: null,
        preTapX: 0,
        preTapY: 0
      },
      {
        el: document.getElementById('leg2'),
        anchorX: clauditoX + 10,
        anchorY: clauditoY + 34,
        hoverX: 108,
        hoverY: 60,
        keys: keyRegions[2],
        noise: {
          curl1: new PerlinNoise(3),
          curl2: new PerlinNoise(13),
          sway: new PerlinNoise(23),
          hover: new PerlinNoise(33),
          keySelect: new PerlinNoise(43)
        },
        phase: 2.7,
        tapping: false,
        tapProgress: 0,
        tapDuration: 0.42,
        lastTapTime: 0.18,
        nextTapDelay: 0.07,
        targetKey: null,
        preTapX: 0,
        preTapY: 0
      },
      {
        el: document.getElementById('leg3'),
        anchorX: clauditoX + 18,
        anchorY: clauditoY + 34,
        hoverX: 120,
        hoverY: 60,
        keys: keyRegions[3],
        noise: {
          curl1: new PerlinNoise(4),
          curl2: new PerlinNoise(14),
          sway: new PerlinNoise(24),
          hover: new PerlinNoise(34),
          keySelect: new PerlinNoise(44)
        },
        phase: 4.1,
        tapping: false,
        tapProgress: 0,
        tapDuration: 0.44,
        lastTapTime: 0.25,
        nextTapDelay: 0.09,
        targetKey: null,
        preTapX: 0,
        preTapY: 0
      }
    ];

    let time = 0;
    let lastTime = performance.now();

    function animate(currentTime) {
      // Skip animation when paused (set by parent page's IntersectionObserver)
      if (window.isPaused) {
        lastTime = currentTime;
        requestAnimationFrame(animate);
        return;
      }
      const deltaTime = (currentTime - lastTime) / 1000;
      lastTime = currentTime;
      time += deltaTime;

      tentacles.forEach(t => {
        const p = t.phase;
        const n = t.noise;

        // Hover movement (when not tapping)
        const hoverOffsetX = n.hover.noise1D(time * 0.4 + p) * 15;
        const hoverOffsetY = n.hover.noise1D(time * 0.5 + p + 100) * 8;

        // Handle tap timing
        if (!t.tapping) {
          if (time - t.lastTapTime > t.nextTapDelay) {
            // Start a new tap - select target key
            const keyNoise = n.keySelect.noise1D(time * 2 + p);
            const keyIndex = Math.floor((keyNoise * 0.5 + 0.5) * t.keys.length);
            t.targetKey = t.keys[Math.min(keyIndex, t.keys.length - 1)];

            // Store pre-tap position
            t.preTapX = t.hoverX + hoverOffsetX;
            t.preTapY = t.hoverY + hoverOffsetY;

            t.tapping = true;
            t.tapProgress = 0;
            // Mostly continuous typing with occasional pauses
            const pauseChance = n.keySelect.noise1D(time * 0.3 + p + 50);
            if (pauseChance > 0.7) {
              // Occasional pause (thinking moment)
              t.nextTapDelay = 0.4 + (pauseChance - 0.7) * 2.0;
            } else {
              // Continuous typing - very short delay
              t.nextTapDelay = 0.02 + Math.random() * 0.08;
            }
          }
        } else {
          t.tapProgress += deltaTime / t.tapDuration;
          if (t.tapProgress >= 1) {
            t.tapping = false;
            t.tapProgress = 0;
            t.lastTapTime = time;
          }
        }

        // Calculate tip position
        let tipX, tipY;

        if (t.tapping && t.targetKey) {
          const tap = tapCurve(t.tapProgress);

          // Interpolate from pre-tap position to target key
          tipX = lerp(t.preTapX, t.targetKey.x, tap.reach);
          tipY = lerp(t.preTapY, t.targetKey.y, tap.reach);

          // During anticipation, pull back slightly
          if (tap.reach < 0) {
            tipY += tap.reach * 10; // Pull up
          }
        } else {
          // Hovering
          tipX = t.hoverX + hoverOffsetX;
          tipY = t.hoverY + hoverOffsetY;
        }

        // Calculate bezier curve
        const dx = tipX - t.anchorX;
        const dy = tipY - t.anchorY;
        const dist = Math.sqrt(dx * dx + dy * dy);

        const perpX = -dy / dist;
        const perpY = dx / dist;

        // Get tap curl modifier
        const tapCurl = t.tapping ? tapCurve(t.tapProgress).curl : 0;

        // Organic curl with tap modifier
        const baseCurl1 = n.curl1.noise1D(time * 1.0 + p) * 16;
        const baseCurl2 = n.curl2.noise1D(time * 1.3 + p + 10) * 20;

        const curl1 = baseCurl1 * (1 + tapCurl);
        const curl2 = baseCurl2 * (1 + tapCurl * 0.6);

        const sway1 = n.sway.noise1D(time * 0.5 + p) * 5;
        const sway2 = n.sway.noise1D(time * 0.7 + p + 5) * 7;

        const cp1x = t.anchorX + dx * 0.33 + perpX * curl1 + sway1;
        const cp1y = t.anchorY + dy * 0.33 + perpY * curl1;

        const cp2x = t.anchorX + dx * 0.66 + perpX * curl2 + sway2;
        const cp2y = t.anchorY + dy * 0.66 + perpY * curl2;

        const path = `M ${t.anchorX} ${t.anchorY} C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${tipX} ${tipY}`;
        t.el.setAttribute('d', path);
      });

      requestAnimationFrame(animate);
    }

    requestAnimationFrame(animate);
  ]]></script>
</svg>